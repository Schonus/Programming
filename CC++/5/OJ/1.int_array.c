#include <stdio.h>
int main(){
    int count=0;
    scanf("%d",&count);
    for(int i=0,array[100]={0},two_count=0;++i<=count?scanf("%d",&array[i-1]):(printf("%d\n",two_count))&&0;array[i-1]==2?two_count++:0){}
    return 0;
}